# 仕様書（設計メモ）：<AIアシスタントの名前>

> フェーズ3でこの形にまとめ、ユーザーの承認を得る。承認後はそのまま構築の元になる。
> **上半分（1〜6）は業務の言葉で・人と合意する部分**。下半分（技術メモ）は Bob 用の裏方。
> 固有名（テンプレ名・ID等）は**推測せず、ユーザー提供の値か対象組織で確認した実値**を入れる。
> 「話が発散したとき、ここに戻って収束させる」ための軸。空欄を埋めながら詰める。

## 1. ねらい（ユーザー価値）★ここから始める
- **誰の**：<例：営業担当>
- **どんな困りごとを**：<例：過去の似た商談を探すのに時間がかかる>
- **どう楽にする**：<例：ヒアリング課題を入れると、似た事例と学びがすぐ出る>
- **ビフォー→アフター**：<例：30分の手探り → 1分で要点>

## 2. 使う人と場面
- 使う人：[ ] 社内（社員）　[ ] 社外（お客様）
- 使う場面・チャネル：<例：商談前に社内画面で。お客様向けならチャット窓口 等>

## 3. やること（できることの「型」に翻訳）
> [../references/できること・できないこと.md](../references/できること・できないこと.md) の6つの型から選ぶ。各アイデアに ✅/⚠️/❌ を付ける。

| やりたいこと | 型（1調べる/2作る/3記録/4処理/5外部/6引継ぎ） | 判定 ✅できる/⚠️要準備/❌範囲外 |
|---|---|---|
| <例：似た商談を探す> | 1 調べる | ⚠️ 検索の準備が要る |
| <例：学びを文章で出す> | 2 作る | ✅ |

## 4. スコープ（やること / やらないこと）
- **やること**：<箇条書き>
- **やらないこと（今回は対象外）**：<例：提案書の自動生成、外部○○連携 ＝発散防止に明示>

## 5. 担当（トピック）と道具・前提
| 担当 | 何をする | 使う道具 | ①で要る準備（人間） | 準備状況 |
|---|---|---|---|---|
| <担当1> | <何をする> | <プロンプト/フロー/Apex/外部/標準> | <検索・テンプレ・接続 等の名前> | [ ] 確認済み |

（標準で「受付」「範囲外のとき」「あいまいなとき」の担当も自動で付きます）

## 6. 入力・出力・会話の流れ・できない時
- **入力（AIが受け取る情報）**：<例：ヒアリング課題のテキスト／商談ID>
- **出力（返す形・見せ方）**：<例：日本語・箇条書き＋表・簡潔>
- **会話の流れ（代表例1〜2）**：<例：ユーザー「〇〇な課題で似た事例は？」→ AI「3件あります…」>
- **できない依頼が来たら**：<例：作り話をせず、正直に断り・人手の手順を案内>

## 7. 成功の目安（これが出たら合格）
- <例：「基幹システムの保守コストが高い」で送って、似た商談が具体的に3件返る>
- <例：空回答・誤リンクが出ない>

## 8. 未決事項・人に確認すること
- <例：検索テンプレの動作確認は？（①）／お客様向けの窓口は？／対象外の線引き>

---

## 技術メモ（内部用・ユーザーには見せない）
- developer_name（API名）：`<Api_Name>`（bundle ディレクトリ名と完全一致）
- agent_type：`AgentforceEmployeeAgent` / `AgentforceServiceAgent`（[../references/エージェント種別と前提.md](../references/エージェント種別と前提.md)）
- 各道具の target：`generatePromptResponse://<Template>` / `flow://<Flow>` / `apex://<Class>` / `externalService://<Service>`
- プロンプト道具の入力：`"Input:<入力名>"` で宣言＋`with`/`set`（ガードレール#5,#12）／`outputs:`必須（#13）
- 検索入り新規テンプレを作る場合のみ：deploy 後に `<activeVersionIdentifier>` で**有効化**（#11）
- ガードレール適用：#1 `is_used_by_planner`+`set` / #2 種別構造 / #3 構文 / #4 locale en_US（複数道具・サービス時は #13〜#20）
