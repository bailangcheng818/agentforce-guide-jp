# 設計書：<AIアシスタントの名前>

> フェーズ3でこの形にまとめ、**ユーザーの承認を得る（承認ゲート）**。承認後はそのまま構築（フェーズ4）の元になる。
> **1〜6 は業務の言葉で・人と合意する部分**。最後の「技術メモ」は Bob 用の裏方（ユーザーには見せない）。
> 固有名（テンプレ名・レトリーバー参照・ID 等）は**推測せず、ユーザー提供の値か対象組織で確認した実値**だけを入れる。
> 「話が発散したとき、ここに戻って収束させる」ための軸。空欄を埋めながら詰める。

---

## 1. ユーザー価値（このエージェントは何の課題を解決するのか）

- **誰の**：<例：営業担当>
- **どんな課題を**：<例：過去の似た商談を探すのに時間がかかる>
- **どう解決する**：<例：ヒアリング課題を入れると、似た事例と学びがすぐ出る>
- **ビフォー → アフター**：<例：30分の手探り → 1分で要点>

## 2. 使う人・場面

- **社内 / 社外**：[ ] 社内（社員＝従業員向け）　[ ] 社外（お客様＝サービス向け）
  - → 種別が決まる（[../references/エージェント種別と前提.md](../references/エージェント種別と前提.md)、ガードレール#2）
- **場面・チャネル**：<例：商談前に社内画面で／お客様向けチャット窓口 等>
- **公開範囲（権限セットで決める）**：<例：営業部の権限セットを持つ人だけ＝誰がこのエージェントと道具を使えるか。ガードレール#16>

## 3. スコープ

- **アクション・やること**：<箇条書き。各項目は「できることの型」に翻訳（[../references/できること・できないこと.md](../references/できること・できないこと.md) の6つの型）し ✅/⚠️/❌ を付ける>

  | やりたいこと（アクション） | 型（1調べる/2作る/3記録/4処理/5外部/6引継ぎ） | 判定 ✅できる/⚠️要準備/❌範囲外 |
  | -------------------------- | --------------------------------------------- | --------------------------------- |
  | <例：似た商談を探す>       | 1 調べる                                      | ⚠️ 検索の準備が要る             |
  | <例：学びを文章で出す>     | 2 作る                                        | ✅                                |

- **やらないこと（今回は対象外）**：<例：提案書の自動生成、外部○○連携 ＝発散防止に明示>
- **トピック外にすべきこと（断る・受け付けない依頼）**：<例：価格の最終決定、与信判断、医療/法務助言 等。「できない依頼が来たら作り話をせず正直に断り、人手の手順を案内」が既定。ガードレール参照>

## 4. 入出力（IPO）

### 4-1. Input：AI が受け取る情報

- **ユーザーインプット**：<例：ヒアリング課題のテキスト／質問文。各入力名は技術メモの `Input:xxx` と対応>
- **Salesforce レコード**：[ ] 使わない　[ ] 単一レコード（<例：表示中の商談1件>）　[ ] 複数レコード（<例：関連する活動履歴>）
- **検索（RAG）**：[ ] 使わない　[ ] 使う（レトリーバー名：`<推測しない・実値>` ／ 対象：<例：商談ナレッジ>）

### 4-2. Process：処理（タスク）

- <例：入力課題でレトリーバーを検索 → 上位事例を取得 → 学びを日本語で要約>
- <担当（サブエージェント）が複数なら、どの入力をどの担当が処理するかも明記>

### 4-3. Output：返す形・見せ方

- **形式**：<例：日本語・箇条書き＋表・簡潔>
- **見せ方/UX**：<例：要点3つ＋出典つき。長文は折りたたみ 等>

## 5. 構成

> 標準で「受付」「範囲外のとき」「あいまいなとき」の担当は自動で付く。ここには**業務の担当（サブエージェント）**を書く。

| サブエージェント（担当） | 役割 | 登録するアクション（道具） | レトリーバー | プロンプトテンプレ | ①で要る準備（人間）／準備状況 |
| ------------------------ | ---- | -------------------------- | ------------ | ------------------ | ----------------------------- |
| <担当1>                  | <何をする> | <プロンプト/フロー/Apex/外部/標準> | `<参照・実値 or なし>` | `<API名・実値 or なし>` | <接続/権限 等>／[ ] 確認済み |
| <担当2>                  | … | … | … | … | … |

- **道具と前提の対応**：各道具が①で用意・動作確認済みかを必ず確認（[../references/前提チェック.md](../references/前提チェック.md)）。未了なら**フェーズ1へ差し戻す**。

## 6. 会話例

### 6-1. 全体の代表シナリオ（エージェント1つとして）

```
ユーザー：<例：〇〇な課題に似た事例はある？>
AI：<例：3件あります。①…／②…／③…。学びは…>
```

### 6-2. サブエージェントごとのシナリオ

**<担当1>**
```
ユーザー：<例：…>
AI：<例：…>
```

**<担当2>**
```
ユーザー：<例：…>
AI：<例：…>
```

- **できない依頼が来たら（トピック外）**：
```
ユーザー：<例：対象外の依頼>
AI：<例：作り話をせず正直に断り、人手の手順を案内>
```

---

## 技術メモ（内部用・ユーザーには見せない）

- developer_name（API名）：`<Api_Name>`（bundle ディレクトリ名と完全一致）
- agent_type：`AgentforceEmployeeAgent` / `AgentforceServiceAgent`（[../references/エージェント種別と前提.md](../references/エージェント種別と前提.md)）
- 各道具の target：`generatePromptResponse://<Template>` / `flow://<Flow>` / `apex://<Class>` / `externalService://<Service>`
- プロンプト道具の入力：`"Input:<入力名>"` で宣言＋`with`/`set`（ガードレール#5,#12）／`outputs:`必須（#13）
- レトリーバー／RAG：使う場合は実値の参照名を控える。検索入り新規テンプレを作る場合のみ、deploy 後に `<activeVersionIdentifier>` で**有効化**（#11）
- 公開範囲：権限セット（誰がエージェント・道具を使えるか）の割当漏れに注意（#16）
- ガードレール適用：#1 `is_used_by_planner`+`set` / #2 種別構造 / #3 構文 / #4 locale en_US（複数道具・サービス時は #13〜#20）
